// submission module
	// Submission model (name, url, title, desc)
		// this.votes
	// contest (submission, submission)
		// vote for
		// loser


// Server-side
index.html
	// basic stuff? simple landing page
	// hooray for this contest!
submit.html
	// entry form
	// splitter function: if length == 8; no more entries for now; in express routing function
viewentries.html
	// ng-repeat contests
	// voting by POST
winner.html
	// hooray for this guy!!
entries.json
	// interactive functions written in app.js
		// getLength()
		// remove(submission): pull, parse, remove, return
		// add : (pull, parse) add (, return)